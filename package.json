{
  "type": "module",
  "scripts": {
    "api:init": "elm-gql init http://localhost:4000/graphql",
    "api:refresh": "elm-gql run ./api/Api/schema.json",
    "graphql-server": "node ./graphql-server.js",
    "build": "elm make --optimize --output elm.js src/Main.elm"
  },
  "dependencies": {
    "graphql": "^16.6.0",
    "graphql-yoga": "^3.3.0"
  },
  "devDependencies": {
    "elm-gql": "^0.5.0"
  }
}
